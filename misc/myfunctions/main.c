#include <stdlib.h>
#include <stdio.h>
#include "node_functions.h"

int main(){
    node * P = criaNodeSimlpes();
    insereCaudaListaSimples(P);
    insereCaudaListaSimples(P);
    insereCaudaListaSimples(P);
    insereCaudaListaSimples(P);
    return 0;
}